# CONTEXT

> 领域术语表 — 不含实现细节。术语如有冲突以本文件为准。

## 核心实体

**店铺 (Shop)**：提供餐饮、丽人、休闲娱乐等服务的线下实体商户。核心属性包括名称、分类、商圈、地址、坐标、人均价格、评分、描述、标签、推荐场景。

**探店笔记 (Blog)**：用户发布的探店/消费评价，包含标题、正文、图片和关联店铺。充当 RAG 中用户评价维度的检索素材。

**优惠券 (Voucher)**：店铺发布的团购券或代金券。分普通券（不限量）和秒杀券（限量限时）。支付金额和抵扣金额以分为单位存储。

**秒杀券 (Seckill Voucher)**：限量限时优惠券，需要设置提醒/订阅而非自动抢购。关联用户等级限制和库存。

**券订单 (Voucher Order)**：用户购买优惠券的订单记录，含对账状态和退款状态。

**用户 (User)**：平台注册用户，通过手机号登录（sa-token 认证）。拥有昵称、头像、等级、积分等属性。

**商圈 (Area)**：地理位置聚类名称，如"春熙路""太古里""九眼桥"。用于搜索标量过滤。

**店铺分类 (Shop Type)**：两级分类树。一级（如"美食"）+ 二级子类（如"川渝火锅"）。通过 `parent_id` 构建父子关系。

**智能导购 (AI Shopping Guide)**：基于自然语言对话为用户推荐店铺和优惠、并可直接下单的 AI Agent。

## 系统边界

- **Java 后端**：业务主数据管理 + 用户认证 + 流式透传代理
- **Python AI 服务**：LLM 对话 + RAG 检索 + 意图识别 + 回复生成
- **Milvus**：向量语义检索（店铺描述 + 用户笔记），不做券向量化
- **Redis**：短期对话历史缓存（30min TTL，自动续期）

## 意图分类

- **recommend_shop**：店铺推荐（需要 Milvus 检索）
- **chat**：闲聊兜底（直接 LLM 回复）
- **purchase**：下单购买券（查券 → 确认 → 下单流程）
